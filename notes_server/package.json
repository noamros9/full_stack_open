{
  "name": "backend",
  "version": "1.0.0",
  "description": "example for full stack project",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../full-stack-open-project && npm run build && @powershell Copy-Item dist -Recurse ../notes_server",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push origin part3"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "mongoose": "^9.6.2"
  }
}
